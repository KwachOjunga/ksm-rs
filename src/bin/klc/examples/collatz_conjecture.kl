func main(startNumber) {
        const totalSteps  = Collatz(startNumber);
        return totalSteps;
}

func Collatz(n) {
    const steps = 0;
	while (n > 1) {
		const v = n % 2;
		if (v == 0) {
			n = n / 2;
		} else {
			const x = 3*n;
			n = x + 1;
		}
		steps = steps + 1;
	}

	return steps;
}
