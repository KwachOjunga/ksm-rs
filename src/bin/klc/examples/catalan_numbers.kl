func CatalanIterative(n) {
	if n <= 1 {
		return 1;
	}

	const res  = 1;
	for i := 1; i <= n; i++ {
		res = res * (4*i-2) / (i+1)
	}
	return res;
}

func main(n) {
	const v = 0;
	for i := 0; i < n; i++ {
	    v =  CatalanIterative(i);
	}
	return v;
}
